
<div class="form_heading">
<br/>
<br/>
Your venues
</div>

<table class="center">
  <tr>
    <th>Name</th>
    <th>Contact</th>
    <th>Phone</th>
    <th></th>
    <th></th>
  </tr>

<% @venues.each do |venue| %>
  <tr>
    <td><%= venue.name %></td>
    <td><%= venue.contact %></td>
    <td><%= venue.phone %></td>
    <td><%= link_to 'Show', venue %></td>
    <td><%= link_to 'Leave', "/users/leave_venue/#{venue.id}" %></td>
  </tr>
<% end %>
</table>

<br />
<div id="footerlinks">
<%= link_to 'New Venue', new_venue_path %>
</div>

<% if  @venues.count == 0 %>
<div id="notes">
You typically will only have one venue.  If you hire musicians at more than one venue, you can add more than one. 
</div>
<% end %>

<% if @avail_venues.count > 0 %>
<div id="notes">
	If you are one of the following venues, you can choose it below.<br/>
	<table>
		<% @avail_venues.each do |v| %>
			<tr><td><%= link_to v.name, "/users/join_venue/#{v.id}" %></td></tr>
		<% end %>
	</table>	
</div>
<% end %>